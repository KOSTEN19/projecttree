# Локальные зависимости и сборка (дублирует корень для ясности)
node_modules/
dist/
# shadcn UI: компоненты в git нужны для Docker/CI без `ui:pull`. Обновляйте через `npm run ui:pull` локально и коммитьте.
.vite/
*.local
.env
.env.*
